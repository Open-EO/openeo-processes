{
    "id": "resample_spatial",
    "summary": "Resample spatial dimension",
    "description": "Resample spatial dimension. Same dimensionality, lower resolution.",
    "categories": [
        "cubes",
        "aggregate"
    ],
    "parameter_order": ["data", "method", "target"],
    "parameters": {
        "data": {
            "description": "",
            "schema": {
                "type": "object",
                "format": "raster-cube"
            },
            "required": true
        },
        "method": {
            "description": "",
            "schema": {
                "type": "string",
                "default": null
            }
        },
        "target": {
            "description": "",
            "schema": {
                "type": "object",
                "format": "raster-cube"
            },
            "required": true
        }
    },
    "returns": {
        "description": "",
        "schema": {
            "type": "object",
            "format": "raster-cube"
        }
    }
}