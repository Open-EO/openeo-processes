{
    "id": "meta",
    "summary": "Retrieve collection metadata",
    "description": "Get metadata for a collection. All available metadata can be retrieved with the openEO Data Discovery for Collections.",
    "categories": [
        "cubes"
    ],
    "parameter_order": ["id", "property"],
    "parameters": {
        "id": {
            "description": "The collection id.",
            "schema": {
                "type": "string",
                "pattern": "^[A-Za-z0-9_\\-\\.~\/]+$"
            },
            "required": true
        },
        "property": {
            "description": "Name of the metadata property. All available metadata can be retrieved with the openEO Data Discovery for Collections. The fields groupes in `properties` can be accessed directly, too.",
            "schema": {
                "type": "string"
            },
            "required": true
        }
    },
    "returns": {
        "description": "The value for the metadata property or `null` if no information is available.",
        "schema": {
            "description": "Any data type is allowed."
        }
    }
}