{
    "id": "resample_temporal",
    "summary": "Resample the temporal dimension",
    "description": "If dimension is not set, the data cube is expected to have only one temporal dimension. Same dimensionality, lower resolution.",
    "categories": [
        "cubes",
        "aggregate"
    ],
    "parameter_order": ["data", "method", "target", "dimension"],
    "parameters": {
        "data": {
            "description": "",
            "schema": {
                "type": "object",
                "format": "raster-cube"
            },
            "required": true
        },
        "method": {
            "description": "",
            "schema": {
                "type": "string",
                "default": null
            }
        },
        "target": {
            "description": "",
            "schema": {
                "type": "object",
                "format": "raster-cube"
            },
            "required": true
        },
        "dimension": {
            "description": "",
            "schema": {
                "type": "string"
            }
        }
    },
    "returns": {
        "description": "",
        "schema": {
            "type": "object",
            "format": "raster-cube"
        }
    }
}