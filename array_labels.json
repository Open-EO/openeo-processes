{
    "id": "array_labels",
    "summary": "Get the labels for an array",
    "description": "Gives all labels for a labeled array in the data cube. The labels have the same order as in the array.\n\nIf the array is not labeled, an empty array is returned.",
    "categories": [
        "arrays"
    ],
    "parameters": [
        {
            "name": "data",
            "description": "An array.",
            "schema": {
                "type": "array"
            }
        }
    ],
    "returns": {
        "description": "The labels as array.",
        "schema": {
            "type": "array",
            "items": {
                "anyOf": [
                    {
                        "type": "number"
                    },
                    {
                        "type": "string"
                    }
                ]
            }
        }
    }
}